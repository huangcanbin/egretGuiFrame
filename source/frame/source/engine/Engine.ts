namespace engine
{
	export import Ticker = egret.Ticker;
	export import ByteArray = egret.ByteArray;
	export import Event = egret.Event;
	export import Timer = egret.Timer;
	export import Sprite = egret.Sprite;
	export import TouchEvent = egret.TouchEvent;
	export import Endian = egret.Endian;
	export import Matrix = egret.Matrix;
	export import Point = egret.Point;
	export import IEventDispatcher = egret.IEventDispatcher;
	export import TimerEvent = egret.TimerEvent;
	export import EventDispatcher = egret.EventDispatcher;
	export import IOErrorEvent = egret.IOErrorEvent;
	export import ProgressEvent = egret.ProgressEvent;
	export import WebSocket = egret.WebSocket;
	export import EventPhase = egret.EventPhase;
	export import Rectangle = egret.Rectangle;
	export import DisplayObject = egret.DisplayObject;
	export import Texture = egret.Texture;
	export import Bitmap = egret.Bitmap;
	export import URLLoaderDataFormat = egret.URLLoaderDataFormat;
	export import URLLoader = egret.URLLoader;
	export import URLRequest = egret.URLRequest;
	export import XML = egret.XML;
	export import XMLText = egret.XMLText;
}